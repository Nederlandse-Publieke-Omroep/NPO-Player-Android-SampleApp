<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <application
        android:name=".SampleApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="${appFlavorName}"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NPOPlayerLibraryAndroid"
        tools:replace="android:label"
        tools:targetApi="32">

        <meta-data
            android:name="BITMOVIN_PLAYER_LICENSE_KEY"
            android:value="${YOUR-PLAYER-LICENSE-KEY}" />
        <meta-data
            android:name="BITMOVIN_ANALYTICS_LICENSE_KEY"
            android:value="${YOUR-ANALYTICS-LICENSE-KEY}" />

        <activity
            android:name=".presentation.SelectNPOTagTypeActivity"
            android:exported="true"
            android:label="${appFlavorNameShort}">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".presentation.MainActivity"
            android:exported="true" />

        <activity
            android:name=".presentation.offline.OfflineActivity"
            android:exported="false" />

        <activity
            android:name=".presentation.player.PlayerActivity"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation"
            android:exported="false"
            android:theme="@style/Theme.NPOPlayer" />
        <!-- Following lines are needed for the usage of cast -->
        <activity
            android:name="com.bitmovin.player.casting.ExpandedControllerActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.CastVideosTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>

            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="nl.npo.player.sampleApp.presentation.MainActivity" />
        </activity>

        <service
            android:name="nl.npo.player.sampleApp.data.offline.service.TestDownloadService"
            android:foregroundServiceType="dataSync" />

        <meta-data
            android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
            android:value="nl.npo.player.sampleApp.presentation.cast.CastOptionsProvider" />
    </application>

</manifest>
